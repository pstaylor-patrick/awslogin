# aws skill repo

This repo is the source of truth for the `/aws` Claude Code skill. Install system-wide with `./install.sh`.

- `SKILL.md`: the skill definition loaded by Claude Code
- `install.sh`: symlinks `~/.claude/skills/aws/SKILL.md` to this repo
