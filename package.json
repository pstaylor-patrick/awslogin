{
  "name": "aws-skill",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Source of truth for the /aws Claude Code skill",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "@vitest/coverage-v8": "^3.2.4"
  }
}
